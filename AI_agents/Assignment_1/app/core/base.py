from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    """Base class that maps our class to a database table

    Args:
        DeclarativeBase (class): Meta data container and a mappper
    """
    pass